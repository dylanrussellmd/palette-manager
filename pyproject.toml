[project]
name = "palette-manager"
version = "0.3.0"
description = "TUI for managing 16-color base16 palettes with configurable apply behavior"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Dylan Russell" }]
dependencies = [
    "textual>=0.50.0",
    "ruamel.yaml>=0.18.0",
    "hsluv>=5.0.0",
]

[project.scripts]
palette-manager = "palette_manager:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/palette_manager"]
